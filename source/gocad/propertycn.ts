/**
 * I don't know what the hell this is but its in a file so
 * just consume it and maybe some doco will show up some day.
 */
export class PropertyCn {
   public isValid: boolean = true;
   public names: any = {};
   public range: any = {
      min: null,
      max: null
   };
}